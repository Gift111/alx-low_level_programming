#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry lowercase alphabets
 *
 * Return: Always 0 (Successful)
 */
int main(void)
{
	char lowch;

	for (lowch = 'a'; low <= 'z'; c++)
		putchar(lowch);
		printf("\n");
	return (0);

}
